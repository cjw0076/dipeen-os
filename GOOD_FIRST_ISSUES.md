# Good First Issues

Use these as initial GitHub issue seeds.

## Demo

- Add a no-provider demo scenario for `Goal -> Run -> Artifact -> Permission -> Memory`.
- Add a Permission Inbox dry-run receipt story.
- Add sample memory candidates for the Control Tower.
- Add a README GIF once the demo route is stable.

## UI

- Add empty states for every Control Tower panel.
- Add loading and error states for canonical control-plane hooks.
- Verify `/office` and `/graph` consume the same state as `/app`.
- Add a screenshot capture script for public launch assets.

## Adapters

- Add an OpenCode provider adapter.
- Add a Gemini CLI provider adapter health check.
- Add adapter contract tests for failure events.
- Add `provider.capabilities()` docs and examples.

## Blueprints

- Add `startup` team blueprint.
- Add `security-audit` team blueprint.
- Add `release-manager` team blueprint.
- Add blueprint validation docs.

## Verifiers

- Add an npm test verifier.
- Add a Playwright verifier.
- Add a secret scan verifier.
- Add a TypeScript build verifier.

## Docs

- Add Dipeen vs OMO comparison.
- Add Dipeen vs Hermes comparison.
- Add a 3-laptop demo runbook.
- Add a Cloudflare named tunnel production guide.
