"""Dipeen demo package — API 키 없이 Product Alpha 가치를 한 명령으로 시연.

`python -m app.demo.product_alpha` — 모든 artifact/receipt는 *진짜*(실제 파일·git diff·실제 reconcile).
가짜 증거를 보이면 'Evidence First' 브랜드와 모순이므로, 결정론적이지만 실제 canonical 경로를 탄다.
"""
